# Set this when Member 1's Express server is ready.
# Leave empty to use in-browser mock data matching API_CONTRACT.md.
VITE_API_URL=

# Member 4 AI (nudge + scheme analyser). Leave empty to use Vite proxy → localhost:5000
VITE_NUDGE_URL=
